# Conflict Resolution Example (Basic)

Conflict:
- Two files specify different memory directory locations.

Resolution:
- Standardize on `OPENCLAW_WORKSPACE_DIR/memory`.
- Update scripts and docs to match.
- Record the decision in GLOBAL-STATE.yaml conflicts as resolved.

