# Example environment overrides for OpenClaw
# Copy to your shell profile or export inline.

# Workspace root for state and memory files:
# export OPENCLAW_WORKSPACE_DIR="$HOME/.openclaw/workspace"

# Optional local-only telemetry (default off):
# export OPENCLAW_TELEMETRY_ENABLED="0"
